pipeline {
    agent { label 'wsl' }
    stages {
        stage('Dependencias y test') {
            steps {
                sh 'echo "saludos desde mi primer pipeline"'
                sh 'node -v'
            }
        }
    }
}